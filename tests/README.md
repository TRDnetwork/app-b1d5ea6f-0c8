How to run:
1. Install dependencies: `npm install vitest jsdom`
2. Run tests: `npm test`

What each file covers:
- `app.test.js`: Unit tests for the frontend app logic.
- `api.test.js`: Tests for Supabase API calls, including table queries and error handling.